# 后端接口 - 正式环境；H5 同域反代可留空，App/小程序必须填 HTTPS 后端域名
SHOPRO_BASE_URL=

# 后端接口 - 测试环境（通过 process.env.NODE_ENV = development）
SHOPRO_DEV_BASE_URL=http://127.0.0.1:48080
SHOPRO_USE_DEV_BASE_URL=true

# 文件上传类型：server - 后端上传， client - 前端直连上传，仅支持 S3 服务
SHOPRO_UPLOAD_TYPE=server

# 后端接口前缀（一般不建议调整）
SHOPRO_API_PATH=/app-api

# 后端 websocket 接口前缀
SHOPRO_WEBSOCKET_PATH=/infra/ws

# 开发环境运行端口
SHOPRO_DEV_PORT=3000

# 客户端静态资源地址：local=本地；生产接 CDN 时再改为正式静态资源域名
SHOPRO_STATIC_URL=local

# 前端 H5 访问域名
SHOPRO_H5_URL=http://127.0.0.1:3000

# 租户ID 默认 1
SHOPRO_TENANT_ID=1
